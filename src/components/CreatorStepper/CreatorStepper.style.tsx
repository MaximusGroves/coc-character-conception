import { makeStyles } from '@material-ui/core'

const styles = makeStyles({
  root: {
    maxWidth: 2000,
    marginLeft: 'auto',
    marginRight: 'auto',
    // padding: 20,
  },
  buttons: {
    paddingLeft: 24,
  },
})

export default styles
